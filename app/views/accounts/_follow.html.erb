<%= form_for(current_account.account_active_relationships.build) do |f| %>
  <div><%= hidden_field_tag :followed_account_id, @account.account_id %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>
