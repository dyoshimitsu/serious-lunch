<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="accounts">
  <% @accounts.each do |account| %>
    <li>
      <%= gravatar_for account, size: 50 %>
      <%= link_to account.account_name, account.account_name %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>
