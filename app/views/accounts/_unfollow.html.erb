<%= form_for(current_account.account_active_relationships.find_by(followed_account_id: @account._account_id),
             html: { method: :delete }) do |f| %>
  <%= f.submit "Unfollow", class: "btn" %>
<% end %>
